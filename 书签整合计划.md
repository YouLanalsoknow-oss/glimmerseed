# GlimmerBook 书签整合计划

## 概述

将书页画布编辑器整合进 3D 工作台，用书签作为统一的 UI 载体重构所有交互面板。书签从视口顶部垂下，分为常驻书签和概念书签两种状态。概念书签点击后成为父级，其他概念书签降为其子级；常驻书签始终保持独立。画布编辑器作为一枚特殊书签，激活时从顶部展开覆盖 3D 视口。

## 设计目标

1. 视口顶部一排书签取代传统工具栏，所有功能入口书签化
2. 画布编辑器与 3D 编辑器共享同一套书签交互体系
3. 常驻书签代表核心模式（3D 视口、画布、导出），概念书签代表功能分类（创建、变换、材质、场景、历史）
4. 书签视觉延续书签样式探索的有机形态与材质纹理

## 书签系统设计

### 常驻书签

常驻书签固定在视口顶部最左侧，始终可见、始终同级，不会成为任何概念书签的子级。点击常驻书签会重置概念书签的父子层级，恢复为默认平级状态。

| 书签 | 形态 | 功能 |
|------|------|------|
| 3D 视口 | 木刻纹理 | 切换到 3D 编辑模式，显示 3D 视口 |
| 画布 | 银杏叶形态 | 展开画布编辑器，从顶部垂下覆盖视口 |
| 导出 | 蜡封形态 | 导出 glTF / 页面 JSON |

### 概念书签

概念书签位于常驻书签右侧，初始全部平级排列。点击任一概念书签后，该书签上升为父级（尺寸增大、位置上移），其余概念书签下降为子级（尺寸缩小、排列在父级下方）。再次点击父级书签或点击常驻书签可重置为平级。

| 书签 | 默认子级内容 | 激活时展开面板 |
|------|-------------|---------------|
| 创建 | 立方体 · 球体 · 圆柱 · 平面 | 子书签直接创建对应几何体 |
| 变换 | 移动 · 旋转 · 缩放 | 子书签切换 Gizmo 模式 |
| 材质 | 颜色 · 金属度 · 粗糙度 | 右侧滑出材质属性面板 |
| 场景 | 大纲 · 图层 | 左侧滑出场景大纲面板 |
| 历史 | 撤销 · 重做 | 子书签直接执行操作 |

### 父子层级交互逻辑

```
初始状态（全部平级）:
[常驻:3D] [常驻:画布] [常驻:导出] | [创建] [变换] [材质] [场景] [历史]

点击"创建"后:
[常驻:3D] [常驻:画布] [常驻:导出] | [创建(父级)]
                                        ├─ 立方体
                                        ├─ 球体
                                        ├─ 圆柱
                                        └─ 平面
                                    [变换] [材质] [场景] [历史] (子级，缩小)

点击"材质"后（切换父级）:
[常驻:3D] [常驻:画布] [常驻:导出] | [材质(父级)]
                                        ├─ 颜色
                                        ├─ 金属度
                                        └─ 粗糙度
                                    [创建] [变换] [场景] [历史] (子级，缩小)
                                    + 右侧滑出材质面板

点击常驻书签（如"3D"）后:
[常驻:3D] [常驻:画布] [常驻:导出] | [创建] [变换] [材质] [场景] [历史] (全部恢复平级)
```

### 视觉风格

延续书签样式探索的四种有机形态与材质：

- **银杏叶**（金色渐变 + 噪点纹理）→ 画布书签
- **丝带火漆**（深红丝带 + 蜡封）→ 导出书签
- **蕨叶**（绿色渐变 + 叶脉纹理）→ 材质类概念书签
- **木刻**（棕色木纹 + 木纹滤镜）→ 3D 视口书签

概念书签使用统一的圆角标签形态，但材质纹理随功能变化：创建类用石质纹理、变换类用金属纹理、场景类用纸质纹理、历史类用墨水质感。

书签状态视觉：
- 默认：半透明、轻微下垂
- 悬停：上浮 4px、轻微摆动动画
- 父级激活：完全不透明、底部发光描边、尺寸放大 1.15 倍
- 子级：尺寸缩小 0.75 倍、半透明、纵向排列在父级下方

### 书签数据模型

```javascript
// 书签定义
type BookmarkDef = {
  id: string,
  type: 'permanent' | 'concept',   // 常驻 or 概念
  label: string,
  icon: string,                     // SVG 形态标识
  material: 'ginkgo' | 'wax' | 'fern' | 'wood' | 'stone' | 'metal' | 'paper' | 'ink',
  children?: BookmarkChild[],       // 概念书签的子级定义
  action: string,                   // 激活时执行的动作标识
  panel?: string,                   // 激活时滑出的面板标识
}

type BookmarkChild = {
  id: string,
  label: string,
  icon: string,
  action: string,
}
```

## 画布编辑器整合

### 下拉展开机制

画布编辑器作为常驻书签"画布"的激活内容。点击画布书签时：

1. 一张纸页画布从视口顶部向下展开，动画时长 400ms
2. 画布展开后覆盖 3D 视口的上半部分（高度可调，默认 55%）
3. 3D 视口缩小到下半部分，保持渲染不中断
4. 画布顶部出现画布专属子书签（选择、文字、图片、形状、网格、缩放）
5. 再次点击画布书签或点击 3D 书签，画布向上收起

画布展开状态支持三种模式：
- **半屏**：画布占视口上半部分，3D 在下半部分
- **全屏**：画布完全覆盖视口，3D 暂停渲染
- **收起**：画布隐藏，仅显示 3D 视口

### 画布工具栏书签化

画布编辑器原有的工具栏按钮全部转换为子书签，挂在画布书签下方：

| 画布子书签 | 功能 |
|-----------|------|
| 选择 | 选择工具（默认激活） |
| 文字 | 添加文字元素 |
| 图片 | 添加图片元素 |
| 形状 | 添加形状元素 |
| 网格 | 切换网格显示 |
| 缩放 | 缩放控制（点击展开缩放滑块） |

### 2D 与 3D 联动

画布编辑的页面内容通过纹理桥接同步到 3D 场景中的书本模型：
- 页面元素拖拽时只更新脏矩形区域
- 松手后异步生成完整页面纹理
- 3D 视口中的书本模型实时接收纹理更新

初期联动策略：画布编辑器独立运行，3D 书本模型作为后续阶段实现。当前阶段先完成画布编辑器的基本功能搬入和书签 UI 整合。

## UI 重构方案

### 现有面板到书签的映射

| 现有组件 | 重构为 | 说明 |
|---------|-------|------|
| Toolbar（工具栏） | 顶部书签条 | 创建/变换/历史按钮 → 概念书签 |
| Outline（大纲面板） | 场景概念书签子级 | 点击"场景"书签时左侧滑出 |
| Inspector（属性面板） | 材质概念书签子级 | 点击"材质"书签时右侧滑出 |
| StatusBar（状态栏） | 底部精简状态条 | 仅保留物体数、选中信息、渲染后端 |
| 画布编辑器 | 画布常驻书签 | 从顶部展开 |

### 新布局结构

```
┌─────────────────────────────────────────────────────┐
│  [3D] [画布] [导出]  │  [创建] [变换] [材质] [场景] [历史]  │  ← 书签条
├─────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────┐  │
│  │              画布编辑区（展开时）                │  │  ← 画布（可展开/收起）
│  │  [选择] [文字] [图片] [形状] [网格] [缩放]      │  │
│  │  ┌─────────────────────────────────────────┐  │  │
│  │  │           2D 页面画布                     │  │  │
│  │  └─────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────┐  │
│  │              3D 视口渲染区                     │  │  ← 3D 视口
│  │                                                │  │
│  └───────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────┤
│  物体: 3 · 已选: 立方体 · 移动模式 · WebGPU        │  ← 精简状态条
└─────────────────────────────────────────────────────┘

侧滑面板（概念书签激活时）:
┌──────────┬─────────────────────────────┬──────────┐
│ 场景大纲  │                             │ 材质属性  │
│ (左侧滑出) │       3D 视口 / 画布        │(右侧滑出) │
│          │                             │          │
└──────────┴─────────────────────────────┴──────────┘
```

## 技术实现

### 新增文件结构

```
工作台/
├── src/
│   ├── core/
│   │   ├── Renderer.js          # 已有
│   │   ├── Viewport.js          # 已有
│   │   ├── SceneManager.js      # 已有
│   │   ├── Commands.js          # 已有
│   │   ├── CanvasManager.js     # 新增：画布文档模型与元素管理
│   │   └── PageModel.js         # 新增：页面数据模型（元素、图层、样式）
│   ├── interaction/
│   │   ├── SelectionController.js   # 已有
│   │   ├── TransformController.js   # 已有
│   │   ├── CanvasInteraction.js     # 新增：画布元素拖拽、选择、缩放
│   │   └── CanvasElement.js        # 新增：画布元素基类（位置、尺寸、旋转）
│   ├── factories/
│   │   ├── GeometryFactory.js      # 已有
│   │   └── ElementFactory.js       # 新增：创建文字/图片/形状元素
│   ├── io/
│   │   ├── Persistence.js          # 已有（扩展保存画布数据）
│   │   └── Exporter.js             # 已有
│   ├── ui/
│   │   ├── BookmarkSystem.js       # 新增：书签系统核心（注册、状态、渲染）
│   │   ├── BookmarkBar.js          # 新增：顶部书签条（常驻+概念排列）
│   │   ├── CanvasPanel.js          # 新增：画布编辑面板（展开/收起/工具）
│   │   ├── SlidePanel.js           # 新增：侧滑面板（大纲/属性）
│   │   ├── Toolbar.js              # 重构：委托给 BookmarkSystem
│   │   ├── Inspector.js            # 重构：嵌入 SlidePanel
│   │   ├── Outline.js              # 重构：嵌入 SlidePanel
│   │   └── StatusBar.js            # 精简
│   └── main.js                     # 重构：初始化书签系统
├── index.html                      # 重构布局
└── styles.css                      # 重构为书签样式系统
```

### BookmarkSystem 核心模块

```javascript
class BookmarkSystem {
  constructor() {
    this.permanent = [];      // 常驻书签定义
    this.concepts = [];       // 概念书签定义
    this.activeParent = null; // 当前父级概念书签 ID
    this.listeners = new Map();
  }

  // 注册书签
  registerPermanent(def) { ... }
  registerConcept(def) { ... }

  // 激活书签
  activate(id) {
    // 常驻书签：重置概念层级 + 执行动作
    // 概念书签：设为父级 + 展开子级 + 滑出面板
  }

  // 重置概念层级
  resetConcepts() { ... }

  // 获取渲染状态
  getRenderState() {
    return {
      permanent: this.permanent,
      concepts: this.concepts.map(c => ({
        ...c,
        isParent: c.id === this.activeParent,
        isChild: this.activeParent !== null && c.id !== this.activeParent,
      })),
      activeParent: this.activeParent,
    };
  }
}
```

### CanvasManager 模块

```javascript
class CanvasManager {
  constructor() {
    this.pages = [];           // 页面列表
    this.currentPage = null;   // 当前编辑页
    this.elements = new Map(); // 元素 ID -> 元素数据
    this.selection = new Set();
    this.zoom = 1;
    this.showGrid = true;
    this._listeners = new Map();
  }

  // 页面管理
  createPage(width, height) { ... }
  selectPage(id) { ... }

  // 元素管理
  addElement(element) { ... }
  removeElement(id) { ... }
  updateElement(id, props) { ... }
  selectElement(id, additive) { ... }

  // 序列化
  getCanvasData() { ... }
  restoreCanvasData(data) { ... }
}
```

### 样式系统

CSS 变量扩展（在现有基础上增加书签相关变量）：

```css
:root {
  /* 现有变量保留 */

  /* 书签形态变量 */
  --bookmark-width: 48px;
  --bookmark-height: 100px;
  --bookmark-child-width: 36px;
  --bookmark-child-height: 72px;
  --bookmark-gap: 6px;

  /* 书签材质色 */
  --ginkgo: #c8b858;
  --wax-red: #9a2830;
  --fern-green: #7a9a52;
  --wood-brown: #8a5a2a;
  --stone-gray: #8a8278;
  --metal-silver: #a0a8b0;
  --paper-cream: #f5ede0;
  --ink-dark: #3a3538;

  /* 书签动画 */
  --bookmark-transition: transform 0.3s cubic-bezier(.2,.8,.2,1), opacity 0.3s ease;
  --bookmark-sway: sway 3.5s ease-in-out infinite;
}
```

书签 SVG 形态使用 `<defs>` 中的滤镜和渐变，与书签样式探索保持一致。

## 实施步骤

### 步骤一：书签系统骨架

- 创建 `BookmarkSystem.js` 和 `BookmarkBar.js`
- 在 `index.html` 中重构布局为书签条 + 视口 + 精简状态条
- 实现常驻书签和概念书签的渲染与基本交互
- 实现父子层级切换逻辑
- 现有 3D 编辑功能通过书签触发，行为不变

验收：书签条正常显示，点击概念书签能切换父子层级，点击常驻书签能重置，所有 3D 编辑功能（创建/变换/选中/删除/撤销重做/导出）通过书签正常工作。

### 步骤二：侧滑面板整合

- 创建 `SlidePanel.js` 通用侧滑面板组件
- 将 Inspector 属性面板重构为"材质"概念书签激活时右侧滑出
- 将 Outline 大纲面板重构为"场景"概念书签激活时左侧滑出
- 面板内容与现有 SceneManager 数据双向绑定保持不变

验收：点击"材质"书签时右侧滑出属性面板，选中物体后面板内容正确；点击"场景"书签时左侧滑出大纲面板；点击常驻书签时面板自动收起。

### 步骤三：画布编辑器搬入

- 创建 `CanvasManager.js`、`PageModel.js`、`CanvasInteraction.js`、`ElementFactory.js`
- 创建 `CanvasPanel.js` 画布面板组件
- 实现画布从顶部展开/收起动画
- 实现画布元素的基本操作：添加文字/图片/形状、拖拽移动、选中、删除
- 画布子书签（选择/文字/图片/形状/网格/缩放）挂在画布常驻书签下

验收：点击画布书签时画布从顶部展开覆盖视口上半部分；能在画布上添加文字、图片、形状元素并拖拽移动；网格可切换；画布数据能保存到 localStorage 并恢复。

### 步骤四：书签视觉材质化

- 将书签的 SVG 形态和材质纹理实现到 CSS/SVG 中
- 四种常驻书签使用对应的有机形态（银杏/蜡封/蕨叶/木刻）
- 概念书签使用统一的圆角标签 + 材质纹理
- 实现悬停摆动动画和父级激活发光效果
- 子级书签的缩小排列和过渡动画

验收：书签视觉与书签样式探索的风格一致；悬停有摆动动画；父级激活有发光描边；子级缩小排列在父级下方；过渡动画流畅无闪烁。

### 步骤五：2D/3D 联动预留

- 画布编辑器数据模型预留纹理输出接口
- 3D 视口预留书本模型挂载点
- 实现画布到 3D 的单向数据流（画布变更 → 通知 3D 更新纹理）
- 本阶段不实现完整 3D 书本模型，仅预留接口

验收：画布元素变更能触发纹理更新事件；3D 视口能接收事件（当前为空操作）；数据流方向正确，画布编辑不依赖 3D 状态。

## 验收标准

1. 视口顶部书签条取代原有工具栏，常驻书签和概念书签视觉区分清晰
2. 点击概念书签后，父级上升、子级下降的层级切换动画流畅（300ms 内完成）
3. 点击常驻书签后，概念书签恢复平级，侧滑面板自动收起
4. 画布书签点击后，2D 画布从顶部展开，3D 视口缩小到下半部分不中断渲染
5. 画布上能添加文字、图片、形状元素，支持拖拽移动、选中、删除
6. 画布工具（选择/文字/图片/形状/网格/缩放）以子书签形式挂在画布书签下
7. 材质属性面板通过"材质"概念书签从右侧滑出，内容与 3D 选中对象实时同步
8. 场景大纲通过"场景"概念书签从左侧滑出，支持选中、删除、查看
9. 刷新后 3D 场景和画布页面数据均能从 localStorage 完整恢复
10. 书签视觉风格与书签样式探索的有机形态和材质纹理一致
11. 所有原有快捷键（1-4 创建、W/E/R 变换、Ctrl+Z 撤销、Delete 删除）保持有效
12. 画布编辑时快捷键切换为画布模式（V 选择、T 文字、I 图片、Space 平移）
